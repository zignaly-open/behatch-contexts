atoum.xunit.xml
behat.yml
behatch_contexts.phar
bin/
composer.lock
composer.phar
vendor

# Ephemeral review-pipeline artifacts (hidden context files); score files themselves (PR-*.json) are tracked
docs/scores/.review-context-PR-*.json
